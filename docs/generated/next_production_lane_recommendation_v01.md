# Next Production Lane Recommendation V01

## Current Position

The project is no longer blocked by missing image stages.

It now has:

- a cleared image-stage backlog
- a documented runtime family system
- ten live chapter-local runtime families
- a compact handoff summary

That means the next production lane should not be:

- more blind family creation
- more backlog cleanup

It should be a controlled usage-expansion lane.

## Recommended Next Lane

Original recommendation:

- `live family usage expansion`

Current status:

- pass 1 is complete
- the recommendation has now moved from `start usage expansion`
  to `choose the next slice deliberately`

This means:

1. review where the current ten live families should actually appear
2. expand only the highest-value surfaces
3. avoid opening more families unless a clearly new contract appears

## Why This Is The Best Next Step

### 1. The family layer is already broad enough

Current live families:

- `well`
- `battery`
- `shrine`
- `floodgate`
- `evidence`
- `bell`
- `chain_control`
- `keeper_lectern`
- `route_marker`
- `latch`

The current risk is no longer under-coverage.

The current risk is uneven usage and drift.

### 2. The next payoff comes from better placement, not more categories

The best immediate gains are:

- clearer briefing usage
- clearer codex / dossier usage
- clearer stage-object identity

Those are usage problems, not family-count problems.

### 3. Shutdown warnings are already isolated

The warning investigation has been narrowed and documented.

That means the main lane can move forward without pretending the warning issue
must be solved first.

## Suggested Execution Order

If the project continues now, use this order:

1. review actual surface use for the live family set
2. promote the highest-value missing surface usages
3. only then reopen new-family questions like `civic_seal`

Current note:

- step 2 delivered a first completed pass
- the next move is not another immediate family opening
- the next move is either a second usage-expansion pass or a deliberate lane transition

## Immediate Candidate Focus

If one concrete follow-up is needed, start with:

- reviewing whether current `briefing-first` and `codex-dossier-first` choices
  are fully reflected in actual authored surfaces

Current status:

- first authored briefing expansion is in place
- first records-based codex-dossier expansion is in place
- selective usage guidance exists for the remaining five live families

That means the next choice is no longer `start usage expansion`.

It is `continue with a new pass` or `switch lanes on purpose`.

That is a more valuable next step than opening another runtime family by default.

## Working Conclusion

The current default should be:

- do not create more system by default
- do not continue pass 1 by inertia
- choose the next slice deliberately
