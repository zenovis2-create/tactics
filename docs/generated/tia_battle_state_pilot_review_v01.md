# Tia Battle-State Pilot Review V01

## Scope

Review target:

- `/Volumes/AI/tactics/docs/generated/tia_battle_state_comparison_board_v01.png`

States:

- `idle`
- `move`
- `attack`

## Verdict

The `Tia` battle-state pilot is good enough to keep as the current layered
candidate set.

## Findings

### 1. `idle` improves ranged-hunter identity

The layered `idle` keeps:

- bow read
- lean stance
- asymmetrical ranger silhouette

more clearly than legacy.

### 2. `move` is materially stronger than legacy

The layered `move` reads:

- more agile
- more grounded
- less generic than the old ranger run

without drifting into rogue acrobatics.

### 3. `attack` is a valid ranged pilot

The layered `attack` reads as:

- bow-led
- ranged first
- lighter than `Rian`

Current judgment:

- acceptable pilot-candidate attack state

## Promotion Guidance

Current pilot-candidate source set:

- `source/tia_idle_sheet_source_v02_layered.png`
- `source/tia_move_sheet_source_v02_layered.png`
- `source/tia_attack_sheet_source_v03_layered.png`

Current runtime-candidate frame export:

- `runtime_v02_layered_candidate/idle/`
- `runtime_v02_layered_candidate/move/`
- `runtime_v02_layered_candidate/attack/`
