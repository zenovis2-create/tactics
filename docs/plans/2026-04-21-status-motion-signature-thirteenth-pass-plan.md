# Status Motion Signature Thirteenth Pass Plan

1. `status_visual_runner.gd`에 motion signature 기대값을 먼저 추가한다.
2. `unit_actor.gd` snapshot에 `status_motion_signature` helper를 추가한다.
3. runner를 다시 실행해 PASS를 확인한다.
