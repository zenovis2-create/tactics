# Lategame Inventory Objective Tenth Pass Plan

1. late-game runner에 inventory objective line 기대값을 먼저 추가한다.
2. 현재 `_get_objective_text()` 기반 inventory snapshot 반영 경로가 relief text를 그대로 쓰는지 확인한다.
3. 필요 시 최소 수정으로 sync를 보정한다.
4. runner를 다시 실행해 PASS를 확인한다.
