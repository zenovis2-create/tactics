# Recall Progress Row Sixth Pass Plan

1. recall hunt integration runner에 progress row 기대값을 먼저 추가한다.
2. `campaign_controller.gd`가 branch/control return card에 progress row를 싣도록 확장한다.
3. `campaign_panel.gd`가 generic presentation card에서도 progress row를 렌더링하도록 확장한다.
4. runner를 다시 실행해 PASS를 확인한다.
