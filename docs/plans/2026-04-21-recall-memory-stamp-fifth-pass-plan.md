# Recall Memory Stamp Fifth Pass Plan

1. recall hunt integration runner에 memory stamp 기대값을 먼저 추가한다.
2. `camp_controller.gd`와 `campaign_controller.gd`에 branch/control stamp payload를 연결한다.
3. `campaign_panel.gd`가 stamp line을 렌더링하도록 확장한다.
4. runner를 다시 실행해 PASS를 확인한다.
