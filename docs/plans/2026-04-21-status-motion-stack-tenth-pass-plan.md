# Status Motion Stack Tenth Pass Plan

1. `status_visual_runner.gd`에 motion stack 기대값을 먼저 추가한다.
2. `unit_actor.gd` snapshot에 `status_motion_stack` helper를 추가한다.
3. 기존 개별 active/profile field와 함께 유지한다.
4. runner를 다시 실행해 PASS를 확인한다.
