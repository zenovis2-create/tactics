# Bond Memory Stamp Fifth Pass Plan

1. support/name-call runner에 memory stamp 기대값을 먼저 추가한다.
2. `campaign_controller.gd`가 support/name-call card에 stamp를 싣도록 확장한다.
3. `campaign_panel.gd`가 stamp line을 전용 memory card surface에 렌더링하도록 확장한다.
4. runner를 다시 실행해 PASS를 확인한다.
